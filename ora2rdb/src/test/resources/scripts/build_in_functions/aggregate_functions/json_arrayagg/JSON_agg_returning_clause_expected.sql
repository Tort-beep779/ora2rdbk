select json_arrayagg (option_r returning varchar(50)) from options;

select json_arrayagg (option_r returning varchar(50)) from options;

select json_arrayagg (option_r) from options;

select json_arrayagg (option_r) from options;