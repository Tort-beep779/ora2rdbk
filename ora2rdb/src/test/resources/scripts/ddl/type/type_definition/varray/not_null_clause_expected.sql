CREATE GLOBAL TEMPORARY TABLE NAMES (
    ID  BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    VAL VARCHAR(15) NOT NULL
);

EXECUTE BLOCK
    AS
    /*TYPE Foursome IS VARRAY(4) OF VARCHAR(15) NOT NULL;*/

    /*team Foursome;*/
BEGIN

END;