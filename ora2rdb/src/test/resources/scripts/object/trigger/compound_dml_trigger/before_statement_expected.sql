/*
 Пример запроса
 insert into emp_test select emp_no, first_name  from employee where dept_no = 671;

 в ORACLE триггер ВСЕГДА срабатывает 1 раз
 в РБД триггер сработает столько же раз , сколько  вставляется новых строк

 */
 /*
  не конвертируется
  */